//area de um quadrado
const promptSync = require("prompt-sync")

const input = promptSync()

parseFloat()

const lado = parseFloat(input("Digite o valor do lado do quadrado: "))

const area = lado * lado

console.log("A area do quadro é igual a: ", area)
